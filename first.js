class Strlength {
 stringlength(string) {
    if(string.length >=1 && string.length <=10){
        return 'error';
  }
  return string.length;
 }

 stringlength(string) {
    return string.length;
   }
}
module.exports = Strlength;